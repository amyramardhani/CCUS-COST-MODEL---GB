{
  "buildCommand": "vite build --outDir dist-vercel --emptyOutDir",
  "outputDirectory": "dist-vercel",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
